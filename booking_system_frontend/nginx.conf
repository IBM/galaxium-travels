server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # No proxy needed - frontend makes API calls directly to /api
    # which the ALB routes to the backend
}

# Made with Bob
